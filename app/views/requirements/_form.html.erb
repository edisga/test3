<%= simple_form_for [@course, @requirement] do |f| %>
  <%= f.input :title, label: 'Título' %>
  <%= f.input :description, label: 'Descripción' %>
  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>