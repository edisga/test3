<h1><%= @course.title %></h1>

<%= link_to "Volver al listado", courses_path %>

<p><%= @course.description %></p>

<%= link_to "Ver modulos", course_moduls_path(@course) %>

<h4>Teacher: <%= @course.teacher.name %></h4>